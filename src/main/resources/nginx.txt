/etc/nginx/nginx.conf : 
# For more information on configuration, see:
#   * Official English Documentation: http://nginx.org/en/docs/
#   * Official Russian Documentation: http://nginx.org/ru/docs/

user root;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

# Load dynamic modules. See /usr/share/doc/nginx/README.dynamic.
include /usr/share/nginx/modules/*.conf;

events {
    worker_connections 1024;
}

http {
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile            on;
    tcp_nopush          on;
    tcp_nodelay         on;
    keepalive_timeout   65;
    types_hash_max_size 2048;

    include             /etc/nginx/mime.types;
    default_type        application/octet-stream;

    # Load modular configuration files from the /etc/nginx/conf.d directory.
    # See http://nginx.org/en/docs/ngx_core_module.html#include
    # for more information.
    include /etc/nginx/conf.d/*.conf;
    include /etc/nginx/sites-enabled/*;
}


/etc/nginx/sites-enabled/default.conf : 

server {
        # SSL configuration
        #
        # listen 443 ssl default_server;
        # listen [::]:443 ssl default_server;
        #
        # Note: You should disable gzip for SSL traffic.
        # See: https://bugs.debian.org/773332
        #
        # Read up on ssl_ciphers to ensure a secure configuration.
        # See: https://bugs.debian.org/765782
        #
        # Self signed certs generated by the ssl-cert package
        # Don't use them in a production server!
        #
        # include snippets/snakeoil.conf;

        listen    80;
        root /root/blog/Records_frontend/build;

        # Add index.php to the list if you are using PHP
        index index.html index.htm index.nginx-debian.html;

        server_name 119.29.60.226; # managed by Certbot

         location  ~ .*\.(gif|jpg|jpeg|png)$ {

            expires 24h;

            root  /root/blog/photos/;#指定图片存放路径

            proxy_store on;

            # proxy_temp_path     /root/blog/photos;#图片访问路径

            proxy_redirect     off;

            proxy_set_header   Host http://119.29.60.226:3000;

            client_max_body_size  100M;

            client_body_buffer_size 1280k;

            proxy_connect_timeout  900;‘

            proxy_send_timeout   900;

            proxy_read_timeout   900;

            proxy_buffer_size    40k;

            proxy_buffers      40 320k;

            proxy_busy_buffers_size 640k;

            proxy_temp_file_write_size 640k;

            if ( !-e $request_filename)
              {
                 proxy_pass http://119.29.60.226:3000; #默认80端口
              }
        }

        location /signin {
                try_files $uri $uri/ /index.html?$args;
        }

        location /home {
                try_files $uri $uri/ /index.html?$args;
        }

        location / {
                try_files $uri $uri/ /index.html?$args;
        }

        location /report {
                proxy_pass http://119.29.60.226:8080;
        }
        location /tool {
                proxy_pass http://119.29.60.226:8080;
        }
        location /note {
             proxy_pass http://119.29.60.226:8080;
        }
        location /directory {
                proxy_pass http://119.29.60.226:8080;
        }
        location /login {
                proxy_pass http://119.29.60.226:8080;
        }
        location /register {
                proxy_pass http://119.29.60.226:8080;
        }
        location /logout {
                proxy_pass http://119.29.60.226:8080;
        }
        location /user {
                proxy_pass http://119.29.60.226:8080;
        }
        location /photo {
                proxy_pass http://119.29.60.226:8080;
        }
        location /photo/upload {
                proxy_pass http://119.29.60.226:8080;
        }
        location /photo/upload/avatar {
                proxy_pass http://119.29.60.226:8080;
        }
        location /photo/upload/cover {
                proxy_pass http://119.29.60.226:8080;
        }

    # listen [::]:443 ssl ipv6only=on; # managed by Certbot
    # listen 443 ssl; # managed by Certbot
    # ssl_certificate /etc/letsencrypt/live/weekly.omsz.io/fullchain.pem; # managed by Certbot
    # ssl_certificate_key /etc/letsencrypt/live/weekly.omsz.io/privkey.pem; # managed by Certbot
    # include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    # ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot
}

server {
    if ($host = 119.29.60.226) {
        return 301 http://$host$request_uri;
    } # managed by Certbot

        listen 80;
        listen [::]:80;
    server_name 119.29.60.226;
    return 404; # managed by Certbot
}
# Settings for a TLS enabled server.
#
#    server {
#        listen       443 ssl http2 default_server;
#        listen       [::]:443 ssl http2 default_server;
#        server_name  _;
#        root         /usr/share/nginx/html;
#
#        ssl_certificate "/etc/pki/nginx/server.crt";
#        ssl_certificate_key "/etc/pki/nginx/private/server.key";
#        ssl_session_cache shared:SSL:1m;
#        ssl_session_timeout  10m;
#        ssl_ciphers HIGH:!aNULL:!MD5;
#        ssl_prefer_server_ciphers on;
#
#        # Load configuration files for the default server block.
#        include /etc/nginx/default.d/*.conf;
#
#        location / {
#        }
#
#        error_page 404 /404.html;
#        location = /404.html {
#        }
#
#        error_page 500 502 503 504 /50x.html;
#        location = /50x.html {
#        }
#    }

nginx 初始配置 : 
# For more information on configuration, see:
#   * Official English Documentation: http://nginx.org/en/docs/
#   * Official Russian Documentation: http://nginx.org/ru/docs/

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /run/nginx.pid;

# Load dynamic modules. See /usr/share/doc/nginx/README.dynamic.
include /usr/share/nginx/modules/*.conf;

events {
    worker_connections 1024;
}

http {
    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    access_log  /var/log/nginx/access.log  main;

    sendfile            on;
    tcp_nopush          on;
    tcp_nodelay         on;
    keepalive_timeout   65;
    types_hash_max_size 2048;

    include             /etc/nginx/mime.types;
    default_type        application/octet-stream;

    # Load modular configuration files from the /etc/nginx/conf.d directory.
    # See http://nginx.org/en/docs/ngx_core_module.html#include
    # for more information.
    include /etc/nginx/conf.d/*.conf;


server {
        listen       80 default_server;
        listen       [::]:80 default_server;
        server_name  _;
        root         /usr/share/nginx/html;

        # Load configuration files for the default server block.
        include /etc/nginx/default.d/*.conf;

        location / {
        }

        error_page 404 /404.html;
        location = /404.html {
        }

        error_page 500 502 503 504 /50x.html;
        location = /50x.html {
        }
    }
 }

